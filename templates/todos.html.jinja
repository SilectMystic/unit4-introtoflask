<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Nunito&display=swap');

        body {
            background-color: rgb(2,0,36);
            color: white;
            font-family: 'Nunito', sans-serif;
            text-align: center;
            margin-left: auto;
            margin-right: auto;
            max-width: 1080px;
            margin-top: 8%;

        }
        ul {
            text-align: center;
            display: inline-block;
        }
        li {
            text-align: center;

        }
        #list{
            background-color: #15192b;
            border-radius: 15px;
        }
        #title{
            background-color: #232946;
            border-radius: 8px;
        }
        hr{
            border: 1px solid #8bd3dd;
        }
        .complete{
            text-decoration: line-through;
            font-style: italic;
        }
        .bnSubmit{
            border-radius: 8px;
            background-color: purple;
        }
        #input{
            k
        }
    </style>
</head>
<body>
    <div id="list">
        <div id="title">
            <h1>Todo List</h1>
            <hr>
        </div>
        <ul>
            {% for todo in todos_list%}
            <li class="{% if todo['complete']%}complete{%endif%}">{{todo['description']}} 
            {% if todo['complete']%}
            🫵
            {%endif%}
            {% if todo['complete']%}
            <form method="post" action="/uncomplete_todo/{{todo['id']}}">
            <input type="submit" class="bnSubmit" value="Uncomplete">
            </form>
            {%endif%}
            {% if todo['complete'] == 0%}
            <form method="post" action="/complete_todo/{{todo['id']}}">
            <input type="submit" class="bnSubmit" value="Complete">
            </form>
            {%endif%}
            <form method="post" action="/delete_todo/{{todo['id']}}">
            <input type="submit" class="bnSubmit" value="Delete">
            </form>
            </li>
            <br>
            {%endfor%}
        </ul>
    </div>
    <form method="post">
        <input name="new_todo" placeholder="Add New Todo!" id="input">
        <input type="submit" class="bnSubmit">
    </form>
</body>
</html>